export default function LoadingSpinner() {
  // TODO: swap for a Framer Motion spinner (lightweight use only, per SAS)
  return (
    <div className="h-4 w-4 animate-spin rounded-full border-2 border-white/20 border-t-white" />
  );
}
